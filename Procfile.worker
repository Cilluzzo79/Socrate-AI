# Procfile for Celery Worker with Healthcheck
# Railway: Add this as separate service for workers
# Use 'web' type so Railway assigns a port and does healthcheck

web: python worker_healthcheck.py & celery -A celery_config worker --loglevel=info --concurrency=2 --queues=documents,maintenance,celery
